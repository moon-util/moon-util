package com.moon.processing.holder;

import com.moon.accessor.annotation.TableModel;

/**
 * @author benshaoye
 */
public enum Table2 {
    ;

    /**
     * 解析表别名，返回一个数组，第一个是别名组名，第二个是别名
     *
     * @param tableModel
     *
     * @return
     */
    public static TableAlias parseAlias(TableModel tableModel) {
        return null;
    }
}
